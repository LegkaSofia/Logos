@model Book
<div class="container">
    <div class="d-inline-flex justify-content-center">
        <div class="card-header">
            @Model.Author
        </div>
        <div class="font-weight-bold card-header">
            @Model.Name
        </div>
    </div>
    <div class="d-inline-flex text-body text-dark">
        @Model.Content
    </div>
</div>